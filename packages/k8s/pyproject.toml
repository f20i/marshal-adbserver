[project]
name = "marshal-adbserver-k8s"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "kubernetes>=32.0.0",
    "pydantic-settings>=2.14.0",
]

[tool.setuptools.package-dir]
marshal_adbserver_k8s = "./src"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
